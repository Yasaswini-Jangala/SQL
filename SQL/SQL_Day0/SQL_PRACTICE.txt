SQL> -- 1.
SQL> SELECT EMP.*,DEPT.*
  2  FROM EMP, DEPT
  3  WHERE EMP.DEPTNO = DEPT.DEPTNO;

SQL> -- 2.
SQL> SELECT EMP.ENAME, DEPT.LOC
  2  FROM DEPT, EMP
  3  WHERE EMP.DEPTNO = DEPT.DEPTNO;

SQL> -- 3.
SQL> SELECT DEPT.*
  2  FROM DEPT
  3  WHERE DEPT.DEPTNO NOT IN (
  4			       SELECT DISTINCT EMP.DEPTNO
  5                            FROM EMP)

SQL> -- 4.
SQL> SELECT DEPTNO,COUNT(DEPTNO)
  2  FROM EMP
  3  GROUP BY EMP.DEPTNO;

SQL> -- 5.
SQL> SELECT EMP.*
  2  FROM EMP
  3  WHERE JOB = (SELECT JOB
  4              FROM EMP
  5              WHERE ENAME = 'SMITH');

SQL> -- 6.
SQL> SELECT D.DNAME, MAX(E.SAL) AS HIGHEST_SALARY
  2  FROM EMP E
  3  JOIN DEPT D ON E.DEPTNO = D.DEPTNO
  4  GROUP BY D.DNAME;

SQL> -- 7.
SQL> SELECT DNAME, AVG(SAL)
  2  FROM EMP INNER JOIN DEPT
  3  ON DEPT.DEPTNO = EMP.DEPTNO
  4  GROUP BY DNAME
  5  HAVING AVG(SAL) > 3000; 

SQL> -- 8.
SQL> SELECT E1.ENAME
  2  FROM EMP E1 JOIN EMP E2
  3  ON E1.MGR = E2.EMPNO
  4  WHERE E1.HIREDATE < E2.HIREDATE;

SQL> -- 9.
SQL> SELECT DEPTNO, COUNT(*) AS EMP_COUNT
  2  FROM EMP
  3  GROUP BY DEPTNO
  4  HAVING COUNT(*) > 2;

SQL> -- 10.
SQL> SELECT DISTINCT D.DEPTNO, D.DNAME
  2  FROM DEPT D
  3  JOIN EMP E ON D.DEPTNO = E.DEPTNO
  4  WHERE E.COMM IS NOT NULL;



